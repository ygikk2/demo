name: my first workflow
on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
            - name: welcome message
              run: | 
                    echo "my first "
                    ls -la
                    cat README.md

            - name: install cowsay
              run: sudo apt-get install cowsay -y

            - name: GENRATE ASCII
              run: cowsay -g dragon "run for cover, I AM A DRAGON..!!!AAHHAA" >> gragon.txt

            - name: GENRATE ASCII
              run: cat gragon.txt

            

          
