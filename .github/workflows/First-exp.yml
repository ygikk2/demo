name: my first workflow
on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
            - name: welcome message
              run: | 
                    echo "my first "
                    ls -la
                    cat README.md

            - name: GENRATE ASCII
              run: cowsay -f dragon "run for cover, I AM A DRAGON..!!!AAHHAA" >> gragon.txt

          
