name: my first workflow
on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
            - name: execute shell
              run: ./asciishell.sh 

            

          
